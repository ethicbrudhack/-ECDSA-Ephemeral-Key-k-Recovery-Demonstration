# -ECDSA-Ephemeral-Key-k-Recovery-Demonstration
This Python snippet demonstrates how to **recompute ephemeral nonces (`k1`, `k2`)** used during the generation of two ECDSA signatures, when the **private key `d`**
